---
interface Props {
  items: { name: string; url: string }[];
}
const { items } = Astro.props;
---

<nav aria-label="Breadcrumb" class="mb-6">
  <ol class="flex flex-wrap items-center gap-1 text-sm text-slate-500 dark:text-slate-400">
    {items.map((item, i) => (
      <li class="flex items-center gap-1">
        {i > 0 && <span class="material-symbols-outlined text-base">chevron_right</span>}
        {i === items.length - 1 ? (
          <span class="text-slate-800 dark:text-slate-200 font-medium">{item.name}</span>
        ) : (
          <a href={item.url} class="hover:text-primary">{item.name}</a>
        )}
      </li>
    ))}
  </ol>
</nav>
